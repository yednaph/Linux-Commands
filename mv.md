<b> Some example usage of the `mv` command </b>

To move a file from one place to another: `mv <source> <destination>`

To move a file from one place to another and automatically overwrite if the destination file exists: `mv -f <source> <destination>`

To move a file from one place to another, but ask before overwriting an existing file: `mv -i <source> <destination>`
